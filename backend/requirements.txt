fastapi
uvicorn[standard]
sqlalchemy>=2.0
greenlet
asyncpg
pydantic
pydantic-settings
openai
pytest
pytest-asyncio
httpx
trueforge-sdk==0.2.0
python-dotenv

# aiosqlite: NOT in the original brief's package list. Added because this sandbox
# cannot resolve DNS for db.<project-ref>.supabase.co (verified: general internet
# DNS works, pypi.org reachable, but the direct Postgres host does not resolve).
# The app's real runtime path uses DATABASE_URL -> postgresql+asyncpg (asyncpg),
# unchanged. Tests point DATABASE_URL at sqlite+aiosqlite so they can actually run
# and pass in this environment; swap DATABASE_URL back to the real Postgres DSN
# once that host is reachable (e.g. outside this sandbox) - no code changes needed.
aiosqlite
